# Public URL of the deployed API. This value is bundled into the production app
# at build time; do not use localhost or a private LAN address for production.
EXPO_PUBLIC_API_URL=https://api.your-company.example
